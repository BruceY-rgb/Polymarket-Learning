# 🚀 Polymarket 数据收集快速开始指南

## 新半年回溯策略 - 3分钟启动！

### ✨ 策略优势
- **从最新数据开始** - 优先获取最近半年的市场数据
- **快速启动** - 首次运行只需 3-5 分钟
- **智能增量** - 后续运行自动从上次停止的地方继续

---

## 🎯 快速开始（3 步骤）

### 步骤 1：测试环境
```bash
cd /Users/yangsmac/Desktop/Polymarket/poly_data

# 测试回溯策略
uv run python test_backward_strategy.py
```

**期望输出**：
```
✅ 测试完成！
📅 预期时间范围:
  当前时间: 2025-01-30 12:34:56 UTC
  回溯起点: 2024-08-03 12:34:56 UTC
  回溯天数: 180
```

### 步骤 2：测试 API 连接
```bash
# 检查 API 是否可用
uv run python test_apis.py
```

**期望输出**：
```
✅ Polymarket API: 正常
✅ Goldsky API: 正常
```

### 步骤 3：开始收集数据
```bash
# 方法 1: 仅收集订单数据（推荐）
uv run python -c "from update_utils.update_goldsky import update_goldsky; update_goldsky()"

# 方法 2: 运行完整管道（包含市场数据和交易处理）
uv run python update_all.py
```

**预期输出**：
```
🔄 从最新数据开始，向前回溯 180 天
🚀 开始抓取 orderFilledEvents
⏳ 获取批次 1...
📝 添加批次 1：1000 条记录
🎉 抓取完成！
📊 总记录数：156,789
```

---

## 📊 预期结果

### 成功运行后您将看到：

| 文件 | 预期大小 | 记录数 | 说明 |
|------|----------|--------|------|
| `markets.csv` | 5-10 MB | 150k+ | 市场元数据 |
| `goldsky/orderFilled.csv` | 30-60 MB | 150k-300k | 半年订单数据 |
| `processed/trades.csv` | 20-40 MB | 150k-300k | 处理后交易 |

### 验证数据
```bash
# 检查文件
ls -lah *.csv goldsky/*.csv processed/*.csv

# 查看记录数
wc -l goldsky/orderFilled.csv

# 查看数据范围
head -n 2 goldsky/orderFilled.csv
tail -n 2 goldsky/orderFilled.csv
```

---

## ⚙️ 自定义选项

### 自定义回溯天数

**30 天（1 个月）**：
```bash
uv run python -c "from update_utils.update_goldsky import update_goldsky; update_goldsky(days_limit=30)"
```

**90 天（3 个月）**：
```bash
uv run python -c "from update_utils.update_goldsky import update_goldsky; update_goldsky(days_limit=90)"
```

**365 天（1 年）**：
```bash
uv run python -c "from update_utils.update_goldsky import update_goldsky; update_goldsky(days_limit=365)"
```

### 数据规模参考

| 回溯天数 | 预计记录数 | 预计运行时间 | 文件大小 |
|----------|------------|--------------|----------|
| 30 天 | ~50,000 | 1-2 分钟 | ~10 MB |
| 90 天 | ~150,000 | 3-5 分钟 | ~30 MB |
| 180 天 | ~300,000 | 5-10 分钟 | ~60 MB |
| 365 天 | ~600,000 | 10-20 分钟 | ~120 MB |

---

## 🔄 增量更新

### 后续运行
管道会自动检测现有数据并从最新点继续：

```bash
# 每周运行一次保持数据最新
uv run python update_all.py
```

**智能检测**：
- ✅ 现有数据较新 → 从最新点继续
- ✅ 数据较旧 → 自动重新回溯
- ✅ 新运行 → 从当前时间回溯

---

## 📖 详细说明

### 核心概念
- **起点**：当前时间（UTC）
- **方向**：向前回溯（从新到旧）
- **范围**：默认 180 天（可配置）
- **排序**：数据按时间戳升序保存

### 工作流程
1. 检测现有数据文件
2. 获取最新时间戳
3. 从当前时间向前查询
4. 按 1000 条记录分批获取
5. 合并、排序、去重
6. 保存为 CSV 文件

---

## 🛠️ 故障排除

### 常见问题

**Q: API 连接失败**
```bash
# 检查网络
ping gamma-api.polymarket.com
ping api.goldsky.com

# 重试
uv run python test_apis.py
```

**Q: 运行时间过长**
```bash
# 减少回溯天数
uv run python -c "from update_utils.update_goldsky import update_goldsky; update_goldsky(days_limit=30)"
```

**Q: 文件损坏**
```bash
# 删除现有数据重新开始
rm -f goldsky/orderFilled.csv
uv run python update_all.py
```

### 获取帮助

| 资源 | 位置 | 说明 |
|------|------|------|
| **策略说明** | `新半年回溯策略说明.md` | 完整策略文档 |
| **故障排除** | `故障排除指南.md` | 详细问题解决 |
| **API 测试** | `test_apis.py` | API 连接测试 |
| **策略测试** | `test_backward_strategy.py` | 回溯策略测试 |
| **完整文档** | `README_zh.md` | 项目完整说明 |

---

## 🎯 使用场景

### ✅ 适合
- 市场监控和分析
- 策略回测
- 快速原型验证
- 资源受限环境

### ❌ 不适合
- 需要数年历史数据
- 市场微观结构研究
- 监管报告要求

---

## 📞 下一步

运行成功后，您可以：

1. **启动 Jupyter 分析**：
   ```bash
   uv run jupyter notebook
   ```

2. **查看示例分析**：
   - `Example 1 Trader Analysis.ipynb`
   - `Example 2 Backtest.ipynb`

3. **自定义分析**：
   - 参考 `笔记本中文说明.md`

---

**🎉 开始您的 Polymarket 数据分析之旅！**

有问题？查看 `故障排除指南.md` 或运行 `test_backward_strategy.py` 进行诊断。
